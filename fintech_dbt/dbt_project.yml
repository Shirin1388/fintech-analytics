name: 'fintech_dbt'
version: '1.0.0'

profile: 'fintech_dbt'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fintech_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table